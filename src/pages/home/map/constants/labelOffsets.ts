export interface LabelOffset {
  x: number;
  y: number;
}

/**
 * 라벨 위치를 손으로 밀어 주는 표.
 *
 * 라벨 자리를 무게중심으로 잡던 시절에는 인천·경기·충북이 경계 쪽으로
 * 치우쳐 손보정이 필요했다. 지금은 최대내접원의 중심에 놓아 경계에서
 * 가장 먼 지점을 자동으로 잡으므로, 그 보정값이 오히려 라벨을 경계
 * 쪽으로 되밀었다(경기 여유 18.04 → 3.52, 인천 3.68 → 1.31). 값을
 * 비우니 겹쳐서 사라지는 라벨도 늘지 않아 전부 걷어냈다.
 *
 * 자동 배치로 해결되지 않는 지역이 생기면 여기에 다시 넣는다.
 */
export const LABEL_OFFSETS: Record<string, LabelOffset> = {};